# ARBITER — Dependencies
aiohttp>=3.9.0
cryptography>=46.0.0
# IB Gateway socket client for ForecastEx (replaces the Client Portal REST
# gateway whose SSO session expires every few hours). Used only when
# IBKR_USE_TWS=true; imported lazily so the REST path keeps working without it.
ib_async>=1.0.0
redis[hiredis]>=5.0.0
asyncpg>=0.29.0
python-dotenv>=1.0.0
web3>=6.0.0
py-clob-client>=0.34.0
structlog>=25.5.0
tenacity>=9.1.4
sentry-sdk>=2.58.0
anthropic>=0.40.0
sentence-transformers>=3.0.0
