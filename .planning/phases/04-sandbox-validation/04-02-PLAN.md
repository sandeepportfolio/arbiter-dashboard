---
phase: 04-sandbox-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - arbiter/config/settings.py
  - arbiter/execution/adapters/polymarket.py
  - arbiter/execution/adapters/test_polymarket_phase4_hardlock.py
  - .env.sandbox.template
  - docker-compose.yml
  - arbiter/sql/init-sandbox.sh
  - .gitignore
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "KalshiConfig.base_url is sourced from KALSHI_BASE_URL env var (default remains production URL); same for KALSHI_WS_URL and POLYMARKET_CLOB_URL"
    - "PolymarketAdapter.place_fok rejects any order where float(qty) * float(price) > float(os.getenv('PHASE4_MAX_ORDER_USD')) with structured log + _failed_order return"
    - "When PHASE4_MAX_ORDER_USD is unset, place_fok proceeds as before (no-op hard-lock branch — production behavior unchanged)"
    - "docker-compose up -d postgres creates both arbiter_dev AND arbiter_sandbox databases on the same instance"
    - ".env.sandbox.template exists as the copy-to-fill operator template and is the single place where all Phase 4 env vars are documented"
    - ".gitignore excludes .env.sandbox and evidence/04/ to prevent credential/balance leakage"
  artifacts:
    - path: "arbiter/config/settings.py"
      provides: "KalshiConfig.base_url + ws_url + PolymarketConfig.clob_url now env-var-sourced"
      contains: "KALSHI_BASE_URL"
    - path: "arbiter/execution/adapters/polymarket.py"
      provides: "PHASE4_MAX_ORDER_USD notional hard-lock in place_fok"
      contains: "PHASE4_MAX_ORDER_USD"
    - path: "arbiter/execution/adapters/test_polymarket_phase4_hardlock.py"
      provides: "Unit tests for hard-lock (5 cases: unset, over, under, boundary, unparseable)"
      contains: "PHASE4_MAX_ORDER_USD"
    - path: ".env.sandbox.template"
      provides: "Operator credential template: DATABASE_URL, KALSHI_*, POLY_*, PHASE4_MAX_ORDER_USD=5"
      contains: "PHASE4_MAX_ORDER_USD=5"
    - path: "docker-compose.yml"
      provides: "POSTGRES_MULTIPLE_DATABASES=arbiter_sandbox + init-sandbox.sh mount"
      contains: "POSTGRES_MULTIPLE_DATABASES"
    - path: "arbiter/sql/init-sandbox.sh"
      provides: "postgres initdb script that creates arbiter_sandbox alongside arbiter_dev"
      contains: "CREATE DATABASE"
    - path: ".gitignore"
      provides: "Excludes .env.sandbox and evidence/04/"
      contains: ".env.sandbox"
  key_links:
    - from: "arbiter/execution/adapters/polymarket.py::place_fok"
      to: "PHASE4_MAX_ORDER_USD env var"
      via: "os.getenv + notional = qty * price comparison"
      pattern: "PHASE4_MAX_ORDER_USD"
    - from: "arbiter/config/settings.py:365"
      to: "KALSHI_BASE_URL env var"
      via: "field(default_factory=lambda: os.getenv(KALSHI_BASE_URL, ...))"
      pattern: "KALSHI_BASE_URL"
    - from: "docker-compose.yml postgres service"
      to: "arbiter/sql/init-sandbox.sh"
      via: "docker-entrypoint-initdb.d mount"
      pattern: "init-sandbox.sh"
---

<objective>
Production-code surgical edits + infrastructure setup to unlock sandbox runs:
1. Source three hardcoded URLs (KalshiConfig.base_url, KalshiConfig.ws_url, PolymarketConfig.clob_url) from env vars so `.env.sandbox` can route traffic to demo/prod without touching code.
2. Add `PHASE4_MAX_ORDER_USD` notional hard-lock to `PolymarketAdapter.place_fok` (adapter-layer belt per D-02; second layer to the $10 test-wallet hardware cap).
3. Add second database (`arbiter_sandbox`) to docker-compose via `POSTGRES_MULTIPLE_DATABASES` init-script pattern.
4. Provide `.env.sandbox.template` operator credential template.
5. Gitignore `.env.sandbox` and `evidence/04/`.

Purpose: This is the only plan touching production code. Every other Phase 4 plan lives under `arbiter/sandbox/` test-only surface. Changes here must be surgical, env-var-opt-in (no-op when env var unset), and backwards-compatible with existing production behavior (Phase 1 through 3 assertions still hold).

Output: 3 config edits + 1 adapter edit with unit tests + docker-compose multi-DB + operator template + .gitignore extension. No new dependencies.
</objective>

<execution_context>
@C:/Users/sande/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sande/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/04-sandbox-validation/04-CONTEXT.md
@.planning/phases/04-sandbox-validation/04-RESEARCH.md
@.planning/phases/04-sandbox-validation/04-PATTERNS.md
@arbiter/config/settings.py
@arbiter/execution/adapters/polymarket.py
@docker-compose.yml
@.env.template
@.gitignore

<interfaces>
<!-- Key contracts from codebase. -->

From arbiter/config/settings.py (around lines 362-384 — EXISTING):
- LINE 365: `base_url: str = "https://api.elections.kalshi.com/trade-api/v2"` (HARDCODED — make env-var-driven)
- LINE ~366: `ws_url: str = "wss://api.elections.kalshi.com/trade-api/ws/v2"` (HARDCODED — make env-var-driven)
- LINE 376: `clob_url: str = "https://clob.polymarket.com"` (HARDCODED — make env-var-driven)
- Existing pattern at lines 367, 378, 383, 384: `field(default_factory=lambda: os.getenv("NAME", "default"))`

From arbiter/execution/adapters/polymarket.py:72-91 (EXISTING — insertion point for hard-lock):
- Guards at lines 73, 79, 86 (private_key, circuit, client-is-None) each return `self._failed_order(...)`
- HARD-LOCK INSERTION POINT: after `client is None` check (line 89), before `return await self._place_fok_reconciling(...)` (line 91)
- `log` structlog logger exists at line 32: `log = structlog.get_logger("arbiter.adapters.polymarket")`
- `_failed_order` helper at lines 632-648 — REUSE, do not invent a new failure path

From docker-compose.yml:12-29 (EXISTING postgres service to extend):
```yaml
postgres:
  image: postgres:16-alpine
  environment:
    POSTGRES_DB: ${PG_DATABASE:-arbiter_dev}
    POSTGRES_USER: ${PG_USER:-arbiter}
    POSTGRES_PASSWORD: ${PG_PASSWORD:-arbiter_secret}
  volumes:
    - pg_data:/var/lib/postgresql/data
    - ./arbiter/sql/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PHASE4_MAX_ORDER_USD notional hard-lock in PolymarketAdapter.place_fok with unit tests</name>
  <files>arbiter/execution/adapters/polymarket.py, arbiter/execution/adapters/test_polymarket_phase4_hardlock.py</files>
  <read_first>
    - arbiter/execution/adapters/polymarket.py (full file — confirm exact line numbers for insertion point; confirm `os` is imported; confirm `log` structlog logger at line 32; confirm `_failed_order` signature at lines 632-648)
    - .planning/phases/04-sandbox-validation/04-RESEARCH.md §Code Examples "Polymarket adapter hard-lock (D-02)" (lines 556-587) — use this EXACT snippet
    - .planning/phases/04-sandbox-validation/04-RESEARCH.md §Pitfall 8 (notional = qty * price, NOT raw qty)
    - .planning/phases/04-sandbox-validation/04-PATTERNS.md §MOD `arbiter/execution/adapters/polymarket.py::place_fok` (shows existing guard pattern and insertion point between client-is-None check at line 89 and `_place_fok_reconciling` call at line 91)
    - .planning/phases/04-sandbox-validation/04-CONTEXT.md §D-02 (belt-and-suspenders mandate)
    - arbiter/safety/conftest.py (fixture factory pattern for adapter mocks in tests)
  </read_first>
  <behavior>
    - When PHASE4_MAX_ORDER_USD unset: place_fok proceeds to _place_fok_reconciling unchanged (backwards compatible)
    - When PHASE4_MAX_ORDER_USD="5" and qty=10, price=0.60 (notional=6.0): place_fok returns _failed_order with error containing "PHASE4_MAX_ORDER_USD hard-lock: notional $6.00 > $5.00" — no HTTP call made
    - When PHASE4_MAX_ORDER_USD="5" and qty=10, price=0.40 (notional=4.0): hard-lock does not trip; proceeds to _place_fok_reconciling (mock)
    - When PHASE4_MAX_ORDER_USD="5" and qty=5, price=1.00 (notional=5.0 — exactly equal): hard-lock does NOT trip (comparison is strict `>`, not `>=`)
    - When PHASE4_MAX_ORDER_USD="not-a-number": float parsing falls back to 0.0, which means ANY positive notional trips — this is safe (defaults to max safety)
    - On rejection, structlog emits `polymarket.phase4_hardlock.rejected` event with `arb_id`, `notional`, `max`, `qty`, `price` keys
  </behavior>
  <action>
**File 1 edit: `arbiter/execution/adapters/polymarket.py`** — surgical edit to `place_fok`.

First, verify `import os` is at the top of the file. If not, add `import os` to the existing import block.

Insert the following block IMMEDIATELY AFTER the `client is None` early-return guard (current line 89 per PATTERNS.md; verify exact line by reading the file) and IMMEDIATELY BEFORE `return await self._place_fok_reconciling(...)` (current line 91):

```
    # Phase 4 blast-radius hard-lock (D-02): adapter-layer belt above RiskManager + test-wallet
    # hardware cap. When PHASE4_MAX_ORDER_USD is unset, this is a no-op (production behavior
    # unchanged). When set, rejects any order whose notional (qty * price) exceeds the cap.
    # Notional is the correct risk measure (Pitfall 8); raw qty is meaningless on prediction markets.
    max_order_usd_raw = os.getenv("PHASE4_MAX_ORDER_USD")
    if max_order_usd_raw:
        try:
            max_order_usd = float(max_order_usd_raw)
        except (TypeError, ValueError):
            max_order_usd = 0.0  # Unparseable -> maximally restrictive (safe default).
        notional_usd = float(qty) * float(price)
        if notional_usd > max_order_usd:
            log.warning(
                "polymarket.phase4_hardlock.rejected",
                arb_id=arb_id,
                notional=notional_usd,
                max=max_order_usd,
                qty=qty,
                price=price,
            )
            return self._failed_order(
                arb_id, market_id, canonical_id, side, price, qty, now,
                f"PHASE4_MAX_ORDER_USD hard-lock: notional ${notional_usd:.2f} > ${max_order_usd:.2f}",
            )
```

**Important:** Use strict `>` comparison, NOT `>=`. A $5.00 notional with a $5 cap should be ALLOWED (operator intent). Only strictly over-cap trips. The test `notional == max_order_usd` case must pass.

**File 2 create: `arbiter/execution/adapters/test_polymarket_phase4_hardlock.py`** — unit tests for the hard-lock:

```
"""Tests for PHASE4_MAX_ORDER_USD hard-lock in PolymarketAdapter.place_fok (Plan 04-02 Task 1)."""
from __future__ import annotations

import os
from unittest.mock import AsyncMock, MagicMock, patch

import pytest

from arbiter.execution.adapters.polymarket import PolymarketAdapter
# NOTE: OrderStatus import path — confirm by inspecting imports in polymarket.py; likely
# `from arbiter.execution.engine import OrderStatus` or `from arbiter.execution.types import OrderStatus`.
from arbiter.execution.engine import OrderStatus  # adjust if needed


def _make_adapter():
    """Build PolymarketAdapter with mocked config + circuit + client so only the hard-lock path exercises."""
    cfg = MagicMock()
    cfg.polymarket.private_key = "0xDEADBEEF" * 8  # pass the private_key guard
    adapter = PolymarketAdapter(cfg)
    # Bypass circuit breaker + client-is-None guards
    adapter.circuit = MagicMock()
    adapter.circuit.can_execute.return_value = True
    adapter._get_client = MagicMock(return_value=MagicMock())  # client is not None
    # Stub _place_fok_reconciling so tests assert hard-lock runs BEFORE reconciler
    adapter._place_fok_reconciling = AsyncMock(return_value=MagicMock(status=OrderStatus.FILLED))
    return adapter


async def test_hardlock_noop_when_env_unset(monkeypatch):
    monkeypatch.delenv("PHASE4_MAX_ORDER_USD", raising=False)
    adapter = _make_adapter()
    await adapter.place_fok("ARB-1", "mkt", "CAN", "yes", 0.5, 100)
    adapter._place_fok_reconciling.assert_awaited_once()


async def test_hardlock_rejects_when_notional_exceeds_cap(monkeypatch):
    monkeypatch.setenv("PHASE4_MAX_ORDER_USD", "5")
    adapter = _make_adapter()
    result = await adapter.place_fok("ARB-2", "mkt", "CAN", "yes", 0.60, 10)
    assert result.status == OrderStatus.FAILED
    assert "PHASE4_MAX_ORDER_USD hard-lock" in result.error
    assert "$6.00" in result.error
    assert "$5.00" in result.error
    adapter._place_fok_reconciling.assert_not_awaited()


async def test_hardlock_allows_when_notional_under_cap(monkeypatch):
    monkeypatch.setenv("PHASE4_MAX_ORDER_USD", "5")
    adapter = _make_adapter()
    await adapter.place_fok("ARB-3", "mkt", "CAN", "yes", 0.40, 10)
    adapter._place_fok_reconciling.assert_awaited_once()


async def test_hardlock_boundary_exact_equal_allowed(monkeypatch):
    """Strict > comparison: notional == cap is ALLOWED (operator intent)."""
    monkeypatch.setenv("PHASE4_MAX_ORDER_USD", "5")
    adapter = _make_adapter()
    await adapter.place_fok("ARB-4", "mkt", "CAN", "yes", 1.0, 5)  # notional = 5.0
    adapter._place_fok_reconciling.assert_awaited_once()


async def test_hardlock_unparseable_env_is_maximally_restrictive(monkeypatch):
    """Garbage env value -> falls back to 0.0 cap -> any positive notional rejected."""
    monkeypatch.setenv("PHASE4_MAX_ORDER_USD", "not-a-number")
    adapter = _make_adapter()
    result = await adapter.place_fok("ARB-5", "mkt", "CAN", "yes", 0.01, 1)
    assert result.status == OrderStatus.FAILED
    assert "hard-lock" in result.error
    adapter._place_fok_reconciling.assert_not_awaited()
```

**Gotchas:**
- `PolymarketAdapter.__init__` may take more than just `config` — inspect the actual signature and adapt `_make_adapter`. The pattern is: build a MagicMock config that satisfies the `getattr(self.config.polymarket, "private_key", None)` check.
- `OrderStatus` import path — if it lives in `arbiter.execution.engine` vs `arbiter.execution.types`, adjust by inspecting actual imports in `polymarket.py`.
- If adapter tests already exist at `arbiter/execution/adapters/test_polymarket.py`, follow the existing file's test style.
- These tests use `monkeypatch` (a standard pytest fixture), NOT `@pytest.mark.asyncio`. The project's root conftest dispatches coroutine tests via `asyncio.run()`.
  </action>
  <verify>
    <automated>cd "C:/Users/sande/Documents/arbiter-dashboard" && python -m pytest arbiter/execution/adapters/test_polymarket_phase4_hardlock.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PHASE4_MAX_ORDER_USD" arbiter/execution/adapters/polymarket.py` returns at least 2 (env-var read + error string)
    - `grep -q "polymarket.phase4_hardlock.rejected" arbiter/execution/adapters/polymarket.py` succeeds (structlog event)
    - `grep -q "notional_usd = float(qty) \* float(price)" arbiter/execution/adapters/polymarket.py` succeeds (notional formula per Pitfall 8)
    - `grep -q "notional_usd > max_order_usd" arbiter/execution/adapters/polymarket.py` succeeds (strict greater-than)
    - `arbiter/execution/adapters/test_polymarket_phase4_hardlock.py` file exists with 5 `async def test_hardlock_*` tests
    - `pytest arbiter/execution/adapters/test_polymarket_phase4_hardlock.py -v` reports 5/5 PASSED
    - `pytest arbiter/execution/adapters/ arbiter/execution/test_engine.py -v` still passes (no regression in existing adapter tests)
  </acceptance_criteria>
  <done>Hard-lock implemented at the adapter layer; 5 unit tests cover unset/over/under/boundary/unparseable cases; no regression in existing execution tests.</done>
</task>

<task type="auto">
  <name>Task 2: Env-var-sourced base URLs in settings.py (KalshiConfig.base_url, KalshiConfig.ws_url, PolymarketConfig.clob_url)</name>
  <files>arbiter/config/settings.py</files>
  <read_first>
    - arbiter/config/settings.py (full context around lines 362-385 — confirm exact line numbers; confirm `import os` is present; confirm `field(default_factory=...)` pattern already exists at lines 367, 378, 383, 384)
    - .planning/phases/04-sandbox-validation/04-RESEARCH.md §Code Examples "Env-var override for base URLs (D-01)" (lines 451-484) — use this EXACT snippet structure
    - .planning/phases/04-sandbox-validation/04-PATTERNS.md §MOD `arbiter/config/settings.py:365, 376` (same-file analog pattern at lines 367, 378, 383, 384)
    - .planning/phases/04-sandbox-validation/04-CONTEXT.md §D-01 (KALSHI_BASE_URL override; default stays production)
  </read_first>
  <action>
Convert three hardcoded URL defaults to env-var-sourced `field(default_factory=...)` using the existing pattern at neighboring lines (367, 378, 383, 384).

**Edit 1: `arbiter/config/settings.py` KalshiConfig.base_url (line 365)**

Replace:
```
    base_url: str = "https://api.elections.kalshi.com/trade-api/v2"
```
With:
```
    base_url: str = field(
        default_factory=lambda: os.getenv(
            "KALSHI_BASE_URL",
            "https://api.elections.kalshi.com/trade-api/v2",
        )
    )
```

**Edit 2: `arbiter/config/settings.py` KalshiConfig.ws_url (adjacent line)**

Replace:
```
    ws_url: str = "wss://api.elections.kalshi.com/trade-api/ws/v2"
```
With:
```
    ws_url: str = field(
        default_factory=lambda: os.getenv(
            "KALSHI_WS_URL",
            "wss://api.elections.kalshi.com/trade-api/ws/v2",
        )
    )
```

**Edit 3: `arbiter/config/settings.py` PolymarketConfig.clob_url (line 376)**

Replace:
```
    clob_url: str = "https://clob.polymarket.com"
```
With:
```
    clob_url: str = field(
        default_factory=lambda: os.getenv(
            "POLYMARKET_CLOB_URL",
            "https://clob.polymarket.com",
        )
    )
```

**Backwards compatibility:** Every edit preserves the exact previous default string. When the env var is unset, behavior is identical to pre-edit. This means existing tests that load `ArbiterConfig()` without setting these env vars continue to produce production URLs.

**Verification that defaults are preserved:** Write a one-shot sanity check using `python -c`:

```
python -c "import os; os.environ.pop('KALSHI_BASE_URL', None); os.environ.pop('KALSHI_WS_URL', None); os.environ.pop('POLYMARKET_CLOB_URL', None); from arbiter.config.settings import KalshiConfig, PolymarketConfig; k = KalshiConfig(); p = PolymarketConfig(); assert k.base_url == 'https://api.elections.kalshi.com/trade-api/v2', k.base_url; assert k.ws_url == 'wss://api.elections.kalshi.com/trade-api/ws/v2', k.ws_url; assert p.clob_url == 'https://clob.polymarket.com', p.clob_url; print('defaults preserved')"
```

**Env-var override sanity check:**

```
python -c "import os; os.environ['KALSHI_BASE_URL']='https://demo-api.kalshi.co/trade-api/v2'; from arbiter.config.settings import KalshiConfig; k = KalshiConfig(); assert k.base_url == 'https://demo-api.kalshi.co/trade-api/v2', k.base_url; print('override works')"
```

**Gotchas:**
- `field` is already imported from `dataclasses` at the top of settings.py (confirm via grep). If not, add it.
- `os` is already imported (confirm). If not, add `import os`.
- Line numbers may drift slightly between when 04-PATTERNS.md was authored and now; use the existing `field(default_factory=lambda: os.getenv(...))` patterns at lines 367, 378, 383, 384 as guideposts to the correct section.
  </action>
  <verify>
    <automated>cd "C:/Users/sande/Documents/arbiter-dashboard" && python -c "import os; os.environ.pop('KALSHI_BASE_URL', None); os.environ.pop('KALSHI_WS_URL', None); os.environ.pop('POLYMARKET_CLOB_URL', None); from arbiter.config.settings import KalshiConfig, PolymarketConfig; k = KalshiConfig(); p = PolymarketConfig(); assert k.base_url == 'https://api.elections.kalshi.com/trade-api/v2', k.base_url; assert k.ws_url == 'wss://api.elections.kalshi.com/trade-api/ws/v2', k.ws_url; assert p.clob_url == 'https://clob.polymarket.com', p.clob_url; print('defaults preserved')" && python -c "import os; os.environ['KALSHI_BASE_URL']='https://demo-api.kalshi.co/trade-api/v2'; os.environ['POLYMARKET_CLOB_URL']='https://clob-test.example'; from arbiter.config.settings import KalshiConfig, PolymarketConfig; k=KalshiConfig(); p=PolymarketConfig(); assert k.base_url.startswith('https://demo-api.kalshi.co'); assert p.clob_url == 'https://clob-test.example'; print('override works')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "KALSHI_BASE_URL" arbiter/config/settings.py` returns at least 1
    - `grep -c "KALSHI_WS_URL" arbiter/config/settings.py` returns at least 1
    - `grep -c "POLYMARKET_CLOB_URL" arbiter/config/settings.py` returns at least 1
    - `grep -q 'default_factory=lambda: os.getenv("KALSHI_BASE_URL"' arbiter/config/settings.py` succeeds
    - Default-preservation sanity check passes (both python -c commands above return exit 0)
    - `pytest arbiter/ -x --ignore=arbiter/sandbox -q` still passes (no config-related regression)
  </acceptance_criteria>
  <done>Three URL defaults are env-var-driven with production defaults preserved; unset-env path still yields production URLs; override-via-env path yields demo URLs.</done>
</task>

<task type="auto">
  <name>Task 3: docker-compose multi-DB init + .env.sandbox.template + .gitignore updates</name>
  <files>docker-compose.yml, arbiter/sql/init-sandbox.sh, .env.sandbox.template, .gitignore</files>
  <read_first>
    - docker-compose.yml (full file — lines 12-29 are the postgres service block to extend)
    - .env.template (full file — 52 lines; copy structure and section-header style)
    - .gitignore (confirm what is already excluded; do not duplicate existing entries)
    - arbiter/sql/init.sql (confirm the existing init mount point convention — proves the directory naming convention `arbiter/sql/`)
    - .planning/phases/04-sandbox-validation/04-RESEARCH.md §Code Examples "docker-compose multi-database init" (lines 519-554) — use the EXACT bash init-script body
    - .planning/phases/04-sandbox-validation/04-RESEARCH.md §Code Examples ".env.sandbox.template" (lines 486-517) — use this as canonical template
    - .planning/phases/04-sandbox-validation/04-CONTEXT.md §D-01, §D-02, §D-03, §D-04
  </read_first>
  <action>
Three infrastructure files and one gitignore update.

**File 1 edit: `docker-compose.yml`** — extend the existing postgres service block.

Locate the postgres service (around lines 12-29). In the `environment:` block, add:
```
      POSTGRES_MULTIPLE_DATABASES: arbiter_sandbox
```
In the `volumes:` block, append a new mount:
```
      - ./arbiter/sql/init-sandbox.sh:/docker-entrypoint-initdb.d/init-sandbox.sh:ro
```

Preserve ALL existing lines — only ADD the env var and the new volume mount. The primary DB (POSTGRES_DB=arbiter_dev) is unchanged; the new init script creates `arbiter_sandbox` alongside it.

**File 2 create: `arbiter/sql/init-sandbox.sh`** — postgres init script (per RESEARCH.md lines 541-554):

```
#!/bin/bash
# Creates additional databases alongside POSTGRES_DB for Phase 4 sandbox validation.
# Mounted at /docker-entrypoint-initdb.d/init-sandbox.sh by docker-compose.
# Only runs on first volume init — subsequent container starts are no-ops.
set -e

if [ -n "$POSTGRES_MULTIPLE_DATABASES" ]; then
    echo "Creating additional databases: $POSTGRES_MULTIPLE_DATABASES"
    for db in $(echo "$POSTGRES_MULTIPLE_DATABASES" | tr ',' ' '); do
        echo "  -> CREATE DATABASE $db"
        psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" <<-EOSQL
            CREATE DATABASE $db;
            GRANT ALL PRIVILEGES ON DATABASE $db TO $POSTGRES_USER;
EOSQL
    done
fi
```

The file MUST have executable permissions on the host for the docker-entrypoint to run it: `chmod +x arbiter/sql/init-sandbox.sh`.

Verify on disk (Linux/macOS): `test -x arbiter/sql/init-sandbox.sh`. On Windows hosts where POSIX mode bits are not set, git will still track the file as executable if we set the mode via `git update-index --chmod=+x arbiter/sql/init-sandbox.sh` — include that command in the task output.

**File 3 create: `.env.sandbox.template`** — use this EXACT content (per RESEARCH.md §Code Examples lines 486-517, with safety comments):

```
# arbiter_sandbox environment — Phase 4 live-fire scenarios
#
# Copy to .env.sandbox and fill in values. Source with:
#   set -a; source .env.sandbox; set +a
# before running `pytest -m live --live arbiter/sandbox/`.
#
# CRITICAL SAFETY NOTES:
# - Polymarket has NO sandbox. This template targets Polymarket production with a
#   THROWAWAY test wallet and a $5 adapter hard-lock. Never paste a prod wallet key here.
# - Kalshi demo is a real service but uses test money. Demo keys are DIFFERENT from prod
#   keys — save the demo key to keys/kalshi_demo_private.pem, NOT keys/kalshi_private.pem.
# - DATABASE_URL MUST point at arbiter_sandbox. Fixtures refuse to run otherwise.

# ── Core ────────────────────────────────────────────────────────────────────
DRY_RUN=false

# ── Postgres (SEPARATE DB from prod) ────────────────────────────────────────
DATABASE_URL=postgresql://arbiter:arbiter_secret@localhost:5432/arbiter_sandbox

# ── Kalshi (DEMO — separate key from prod) ──────────────────────────────────
KALSHI_BASE_URL=https://demo-api.kalshi.co/trade-api/v2
KALSHI_WS_URL=wss://demo-api.kalshi.co/trade-api/ws/v2
KALSHI_API_KEY_ID=<demo-api-key-id-from-demo.kalshi.co>
KALSHI_PRIVATE_KEY_PATH=./keys/kalshi_demo_private.pem

# ── Polymarket (PRODUCTION — throwaway wallet, <=$10 USDC) ──────────────────
POLYMARKET_CLOB_URL=https://clob.polymarket.com
POLY_PRIVATE_KEY=<TEST-WALLET-PRIVATE-KEY-ONLY>
POLY_SIGNATURE_TYPE=2
POLY_FUNDER=<test-wallet-funder-address>

# ── Phase 4 hard-lock (belt-and-suspenders with $10 wallet cap) ─────────────
PHASE4_MAX_ORDER_USD=5

# ── Telegram (optional — fill if you want kill-switch alerts) ───────────────
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
```

**File 4 edit: `.gitignore`** — APPEND these entries if not already present (check first with grep):

```
# Phase 4 sandbox credentials + evidence
.env.sandbox
evidence/04/
keys/kalshi_demo_private.pem
```

The `keys/kalshi_demo_private.pem` entry is defensive; the existing `keys/` entry may already cover it. Check `.gitignore` first and add only the missing lines.

**Gotchas:**
- Heredoc syntax in init-sandbox.sh: `<<-EOSQL` (leading tab strips) vs `<<EOSQL` (no stripping). We use `<<-EOSQL` so indentation works; tabs are required before closing EOSQL.
- On Windows hosts, chmod +x may not persist in the filesystem, but git treats the bit via the index. Use `git update-index --chmod=+x arbiter/sql/init-sandbox.sh` after creation.
- `POSTGRES_MULTIPLE_DATABASES` is NOT a built-in Postgres env var — it is a convention that our init script reads. Do not rely on Postgres to auto-parse it.
- Docker-compose volume mounts are read-only for init scripts (`:ro` suffix). That is already the convention for `init.sql` — apply the same to `init-sandbox.sh`.
  </action>
  <verify>
    <automated>cd "C:/Users/sande/Documents/arbiter-dashboard" && test -f arbiter/sql/init-sandbox.sh && test -f .env.sandbox.template && grep -q "POSTGRES_MULTIPLE_DATABASES" docker-compose.yml && grep -q "init-sandbox.sh" docker-compose.yml && grep -q "arbiter_sandbox" .env.sandbox.template && grep -q "PHASE4_MAX_ORDER_USD=5" .env.sandbox.template && grep -q "^\.env\.sandbox$\|^\.env\.sandbox[[:space:]]*$" .gitignore && grep -q "evidence/04" .gitignore && bash -n arbiter/sql/init-sandbox.sh && echo "ALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains `POSTGRES_MULTIPLE_DATABASES: arbiter_sandbox` inside the postgres service environment block
    - `docker-compose.yml` contains `./arbiter/sql/init-sandbox.sh:/docker-entrypoint-initdb.d/init-sandbox.sh:ro` in the postgres volumes list
    - `arbiter/sql/init-sandbox.sh` exists, starts with `#!/bin/bash`, contains `CREATE DATABASE $db`, and `bash -n arbiter/sql/init-sandbox.sh` (syntax check) succeeds
    - `.env.sandbox.template` exists, contains `DATABASE_URL=postgresql://arbiter:arbiter_secret@localhost:5432/arbiter_sandbox`, `KALSHI_BASE_URL=https://demo-api.kalshi.co/trade-api/v2`, `PHASE4_MAX_ORDER_USD=5`, and `<TEST-WALLET-PRIVATE-KEY-ONLY>` placeholder
    - `.gitignore` contains `.env.sandbox` and `evidence/04/` entries (one per line)
    - Docker validation: `docker-compose config` parses successfully (`docker-compose -f docker-compose.yml config >/dev/null`)
  </acceptance_criteria>
  <done>Multi-DB init script + template + gitignore all in place; docker-compose.yml parses and mounts the init script; operator can copy .env.sandbox.template to .env.sandbox and proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env var -> Polymarket adapter | `PHASE4_MAX_ORDER_USD` controls the hard-lock cap; env var read path is the trust gate |
| Env var -> Kalshi adapter config | `KALSHI_BASE_URL` determines demo vs prod target; wrong value = prod orders on demo keys (401) |
| Operator shell -> .env.sandbox | Secrets (demo Kalshi key, test wallet private key) live in this file |
| Docker init script -> Postgres | `CREATE DATABASE` executed as POSTGRES_USER; runs only on first volume init |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | PolymarketAdapter.place_fok | mitigate | Notional (qty * price) comparison with strict `>` — Pitfall 8 explicit; unparseable env falls back to 0.0 cap (maximally restrictive); unset env is no-op so production is unaffected |
| T-04-02-02 | Information Disclosure | .env.sandbox | mitigate | .gitignore entry added; .env.sandbox.template never contains real values (only `<PLACEHOLDER>` markers); README mandates demo-only creds |
| T-04-02-03 | Information Disclosure | evidence/04/ | mitigate | .gitignore entry added; directory contains DB dumps + structlog (may include balance data) |
| T-04-02-04 | Elevation of Privilege | init-sandbox.sh | mitigate | Runs only under POSTGRES_USER; `set -e` aborts on any command failure; only CREATE + GRANT statements; no cross-DB privilege escalation |
| T-04-02-05 | Spoofing | KALSHI_BASE_URL env var | mitigate | Production default preserved in default_factory; adapter-layer does not assert on URL (fixture layer in Plan 04-01 enforces `demo-api.kalshi.co` for sandbox fixture) |
| T-04-02-06 | Tampering | docker-compose.yml | accept | Local dev config only; deployment CI has separate compose files; single-operator project |
| T-04-02-07 | Information Disclosure | keys/kalshi_demo_private.pem | mitigate | .gitignore pattern covers keys/ directory; template explicitly says "SEPARATE file from keys/kalshi_private.pem" |
| T-04-02-08 | Denial of Service | PHASE4_MAX_ORDER_USD unparseable | mitigate | `max_order_usd = 0.0` fallback turns ANY positive notional into a rejection — defaults to MAX restriction, not MIN. A garbage env value prevents any order, which is the safe failure mode |
</threat_model>

<verification>
- Task 1 verify: pytest runs 5 unit tests; all pass.
- Task 2 verify: python -c asserts default preservation AND env override paths both work.
- Task 3 verify: file existence + grep for required tokens + bash -n syntax check + docker-compose config validation.
- No changes to Kalshi adapter: production behavior on prod URLs unchanged when env vars unset.
- No changes to engine.py, no changes to RiskManager: Phase 4 hard-lock is ADDITIVE to existing SAFE-02 exposure limits.
</verification>

<success_criteria>
- `pytest arbiter/execution/adapters/test_polymarket_phase4_hardlock.py -v` reports 5/5 PASSED
- `pytest arbiter/ --ignore=arbiter/sandbox -x -q` still passes (no regression in existing tests)
- `python -c "from arbiter.config.settings import KalshiConfig; assert KalshiConfig().base_url == 'https://api.elections.kalshi.com/trade-api/v2'"` succeeds (production default preserved when env unset)
- `KALSHI_BASE_URL=https://demo-api.kalshi.co/trade-api/v2 python -c "from arbiter.config.settings import KalshiConfig; assert KalshiConfig().base_url == 'https://demo-api.kalshi.co/trade-api/v2'"` succeeds (env override works)
- `docker-compose -f docker-compose.yml config` parses without errors
- `bash -n arbiter/sql/init-sandbox.sh` succeeds (shell syntax valid)
- `.env.sandbox.template` contains all 12 required env-var entries (DRY_RUN, DATABASE_URL, KALSHI_BASE_URL, KALSHI_WS_URL, KALSHI_API_KEY_ID, KALSHI_PRIVATE_KEY_PATH, POLYMARKET_CLOB_URL, POLY_PRIVATE_KEY, POLY_SIGNATURE_TYPE, POLY_FUNDER, PHASE4_MAX_ORDER_USD, TELEGRAM_BOT_TOKEN/CHAT_ID)
- `.gitignore` contains `.env.sandbox` and `evidence/04/` entries
</success_criteria>

<output>
After completion, create `.planning/phases/04-sandbox-validation/04-02-SUMMARY.md` capturing:
- Actual line numbers edited in settings.py (may drift from 365/376 depending on file state)
- Actual insertion line number for the hard-lock in polymarket.py
- OrderStatus import path used in the test (engine.py vs types.py)
- Whether `import os` had to be added to polymarket.py or was already present
- Whether `git update-index --chmod=+x` was needed on init-sandbox.sh
- Confirmation that full suite `pytest arbiter/ -x -q` passes
- Interface contracts published for downstream plans: env-var names (`KALSHI_BASE_URL`, `PHASE4_MAX_ORDER_USD`), behaviors (strict `>`, env-unset is no-op)
</output>
