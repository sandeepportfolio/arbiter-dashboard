---
phase: 01-api-integration-fixes
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - arbiter/execution/engine.py
  - arbiter/main.py
  - arbiter/api.py
  - arbiter/workflow/__init__.py
  - arbiter/workflow/predictit_workflow.py
  - arbiter/workflow/test_predictit_workflow.py
  - arbiter/execution/test_engine.py
autonomous: true
requirements: [API-05]

must_haves:
  truths:
    - "No PredictIt execution code remains in engine.py, main.py, api.py, or workflow/"
    - "PredictIt collector in arbiter/collectors/predictit.py is untouched and still works"
    - "PredictIt fee functions in scanner/arbitrage.py and settings.py are untouched (needed for scanner)"
    - "No import errors when running the system after workflow removal"
    - "The system starts without PredictItWorkflowManager"
  artifacts:
    - path: "arbiter/workflow/__init__.py"
      provides: "Empty package init (or deleted directory)"
    - path: "arbiter/main.py"
      provides: "Main orchestrator without PredictIt workflow import/task/shutdown"
    - path: "arbiter/api.py"
      provides: "API server without PredictItWorkflowManager import"
  key_links:
    - from: "arbiter/main.py"
      to: "arbiter/workflow/"
      via: "import removed"
      pattern: "no.*PredictItWorkflowManager.*import"
    - from: "arbiter/collectors/predictit.py"
      to: "arbiter/utils/price_store.py"
      via: "still feeds price data (unchanged)"
      pattern: "class PredictItCollector"
---

<objective>
Remove all PredictIt execution and workflow code from the codebase while keeping the read-only PredictIt collector and fee functions intact. Clean break with no dead code per D-12, D-13, D-14.

Purpose: PredictIt has no trading API -- execution code is dead weight that confuses the codebase and creates false workflow entries. Removing it makes the execution paths cleaner for the Polymarket and Kalshi work that follows.

Output: Clean codebase with PredictIt execution/workflow code removed, all imports updated, no dead code stubs.
</objective>

<execution_context>
@C:/Users/sande/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sande/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-integration-fixes/01-RESEARCH.md
@.planning/phases/01-api-integration-fixes/01-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From arbiter/main.py imports (line 31):
```python
from .workflow import PredictItWorkflowManager  # REMOVE
```

From arbiter/main.py usage (lines 125, 154, 185, 221):
```python
workflow = PredictItWorkflowManager(config.alerts)                    # line 125 REMOVE
workflow_manager=workflow,                                             # line 154 CHANGE to workflow_manager=None
asyncio.create_task(workflow.run(...), name="predictit-workflow"),     # line 185 REMOVE
await workflow.stop()                                                 # line 221 REMOVE
```

From arbiter/api.py imports (line 28):
```python
from .workflow import PredictItWorkflowManager, UnwindReason  # REMOVE
```

From arbiter/api.py (line 150):
```python
workflow_manager: Optional[PredictItWorkflowManager] = None,  # CHANGE type to Optional[Any]
```

From arbiter/workflow/__init__.py (lines 1-16):
```python
"""ARBITER workflow package -- PredictIt manual workflow manager."""
from .predictit_workflow import (
    CloseResult, PredictItWorkflowManager, ReminderAlert,
    UnwindInstruction, UnwindReason,
)
```

From arbiter/execution/engine.py PredictIt strings (lines 341, 395):
```python
"Confirm the PredictIt leg in the dashboard before hedging or unwinding."  # line 341
notes=["PredictIt/manual workflow queued"],  # line 395
```

From arbiter/execution/test_engine.py (line 24):
```python
def test_manual_predictit_opportunity_creates_manual_position():  # PredictIt-specific test
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete PredictIt workflow files and clean workflow package</name>
  <files>arbiter/workflow/predictit_workflow.py, arbiter/workflow/test_predictit_workflow.py, arbiter/workflow/__init__.py</files>
  <read_first>
    - arbiter/workflow/__init__.py (full file -- understand what's exported)
    - arbiter/workflow/predictit_workflow.py (first 30 lines -- confirm it's the right file to delete)
    - arbiter/workflow/test_predictit_workflow.py (first 10 lines -- confirm it's the test for the deleted module)
  </read_first>
  <action>
Per D-12 and D-14 (clean break, no dead code):

1. Delete `arbiter/workflow/predictit_workflow.py` (entire 431-line file)
2. Delete `arbiter/workflow/test_predictit_workflow.py` (entire test file)
3. Replace `arbiter/workflow/__init__.py` contents with:
```python
"""ARBITER workflow package."""
```

This removes all PredictIt workflow types (PredictItWorkflowManager, UnwindReason, UnwindInstruction, ReminderAlert, CloseResult) from the package exports. The workflow/ directory stays as a package (empty init) since api.py has an unwind endpoint that may be repurposed later -- but all PredictIt-specific code is gone.
  </action>
  <verify>
    <automated>python -c "import arbiter.workflow; print('workflow package loads OK')" && python -c "import importlib; m = importlib.import_module('arbiter.workflow'); assert not hasattr(m, 'PredictItWorkflowManager'); print('PredictItWorkflowManager removed')"</automated>
  </verify>
  <acceptance_criteria>
    - arbiter/workflow/predictit_workflow.py does NOT exist
    - arbiter/workflow/test_predictit_workflow.py does NOT exist
    - arbiter/workflow/__init__.py exists and does NOT contain "PredictItWorkflowManager"
    - arbiter/workflow/__init__.py does NOT contain "from .predictit_workflow"
    - `python -c "import arbiter.workflow"` exits 0 (package still loads)
  </acceptance_criteria>
  <done>PredictIt workflow files deleted, workflow package init cleaned to empty package</done>
</task>

<task type="auto">
  <name>Task 2: Remove PredictIt workflow references from main.py and api.py</name>
  <files>arbiter/main.py, arbiter/api.py</files>
  <read_first>
    - arbiter/main.py (full file -- understand all PredictIt references: import, instantiation, task creation, shutdown)
    - arbiter/api.py (lines 1-35 for imports, line 150 for type hint, lines 509-542 for unwind endpoint, line 764 for _parse_unwind_reason, lines 855-870 for create_api_server)
  </read_first>
  <action>
In arbiter/main.py:

1. Remove the import (line 31): `from .workflow import PredictItWorkflowManager`

2. Remove the instantiation (line 125): `workflow = PredictItWorkflowManager(config.alerts)`

3. Change the API server call (line 154) from `workflow_manager=workflow,` to `workflow_manager=None,`

4. Remove the task creation (line 185): `asyncio.create_task(workflow.run(lambda: engine.manual_positions), name="predictit-workflow"),`
   Make sure the trailing comma on the previous line (line 184, portfolio.run()) is still correct for the list.

5. Remove the shutdown call (line 221): `await workflow.stop()`

In arbiter/api.py:

6. Remove the import (line 28): `from .workflow import PredictItWorkflowManager, UnwindReason`

7. Change the type annotation (line 150) from:
   `workflow_manager: Optional[PredictItWorkflowManager] = None,`
   to:
   `workflow_manager: Optional[object] = None,`
   (Using `object` since the workflow_manager interface may be repurposed in future phases. The guard at line 509 `if not self.workflow_manager:` already handles None safely.)

8. For the `_parse_unwind_reason` method (line 764) that references `UnwindReason`, replace it with a stub that returns a string:
```python
    @staticmethod
    def _parse_unwind_reason(reason: str) -> str:
        return reason.strip().lower().replace(" ", "_")
```
   This is safe because the unwind endpoint (line 509) is already guarded by `if not self.workflow_manager:` and will return early since workflow_manager is None.
  </action>
  <verify>
    <automated>python -c "from arbiter.main import main; print('main.py imports OK')" && python -c "from arbiter.api import create_api_server; print('api.py imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - main.py does NOT contain `from .workflow import PredictItWorkflowManager`
    - main.py does NOT contain `PredictItWorkflowManager(config.alerts)`
    - main.py does NOT contain `name="predictit-workflow"`
    - main.py does NOT contain `await workflow.stop()`
    - main.py contains `workflow_manager=None`
    - api.py does NOT contain `from .workflow import PredictItWorkflowManager`
    - api.py does NOT contain `from .workflow import` followed by `UnwindReason`
    - api.py contains `workflow_manager: Optional[object] = None`
    - `python -c "from arbiter.main import main"` exits 0
    - `python -c "from arbiter.api import create_api_server"` exits 0
  </acceptance_criteria>
  <done>All PredictIt workflow references removed from main.py and api.py; both modules import cleanly</done>
</task>

<task type="auto">
  <name>Task 3: Clean PredictIt strings in engine.py and update test file</name>
  <files>arbiter/execution/engine.py, arbiter/execution/test_engine.py</files>
  <read_first>
    - arbiter/execution/engine.py (lines 330-400 -- _queue_manual_execution method for PredictIt-specific text)
    - arbiter/execution/test_engine.py (full file -- find PredictIt-specific test cases)
  </read_first>
  <action>
In arbiter/execution/engine.py:

1. Line 341: Change `"Confirm the PredictIt leg in the dashboard before hedging or unwinding."` to `"Confirm the manual leg in the dashboard before hedging or unwinding."`

2. Line 395: Change `notes=["PredictIt/manual workflow queued"]` to `notes=["Manual workflow queued"]`

3. Leave the `_queue_manual_execution` method itself intact -- it handles manual workflows for any platform pair, not just PredictIt.

4. Leave line 264 `predictit_cap=config.scanner.predictit_cap` as-is -- the MathAuditor still uses this for position sizing calculations.

In arbiter/execution/test_engine.py:

5. Rename `test_manual_predictit_opportunity_creates_manual_position` to `test_manual_opportunity_creates_manual_position` and update internal strings:
   - Change `description="Manual PredictIt opportunity"` to `description="Manual opportunity"`
   - Change `yes_platform="predictit"` to keep as-is (predictit is still a valid platform for price signals, the test verifies the manual workflow still routes correctly)
   - Update the notes assertion if it checks for "PredictIt/manual" to check for "Manual" instead

6. If there are other test functions that specifically test PredictIt execution (not manual routing), remove them. The test for manual routing stays since that code path is still active.
  </action>
  <verify>
    <automated>python -m pytest arbiter/execution/test_engine.py -x --tb=short -q && python -c "import arbiter.execution.engine; print('engine.py imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - engine.py does NOT contain the string "PredictIt leg"
    - engine.py does NOT contain the string "PredictIt/manual"
    - engine.py contains "manual leg" or "Manual leg" in the user-facing instruction
    - engine.py contains `notes=["Manual workflow queued"]`
    - test_engine.py does NOT contain function named `test_manual_predictit_opportunity_creates_manual_position`
    - test_engine.py contains function named `test_manual_opportunity_creates_manual_position`
    - `python -m pytest arbiter/execution/test_engine.py -x` exits 0
  </acceptance_criteria>
  <done>PredictIt-specific strings cleaned from engine.py; test renamed and updated; no dead PredictIt references in execution layer</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import chain | Removing modules must not break import resolution for remaining code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service | Import chain breakage | mitigate | Verify all imports resolve cleanly after removal by running `python -c "from arbiter.main import main"` |
| T-01-06 | Repudiation | Dead code hiding bugs | mitigate | Complete removal per D-14 -- no disabled flags or execution stubs that could mask issues |
</threat_model>

<verification>
1. `python -c "from arbiter.main import main"` -- main.py imports cleanly
2. `python -c "from arbiter.api import create_api_server"` -- api.py imports cleanly
3. `python -c "import arbiter.workflow"` -- workflow package loads (empty)
4. `python -m pytest arbiter/execution/test_engine.py -x --tb=short` -- engine tests pass
5. Verify `arbiter/collectors/predictit.py` is unchanged (per D-13)
6. Grep: no "PredictItWorkflowManager" in main.py or api.py
</verification>

<success_criteria>
- PredictIt workflow files deleted (predictit_workflow.py, test_predictit_workflow.py)
- All PredictIt execution imports/references removed from main.py, api.py, engine.py
- PredictIt collector (arbiter/collectors/predictit.py) untouched
- PredictIt fee functions (settings.py, arbitrage.py) untouched
- Clean break: no disabled flags, no execution stubs (per D-14)
- All imports resolve, all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-integration-fixes/01-03-SUMMARY.md`
</output>
