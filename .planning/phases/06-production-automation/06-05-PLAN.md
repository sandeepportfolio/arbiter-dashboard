---
phase: 06-production-automation
plan: 05
type: execute
wave: 2
depends_on: []
autonomous: true
tags: [phase6, mapping, hot-reload]
requirements: [OPS-06]
files_modified:
  - arbiter/api.py
  - arbiter/mapping/market_map.py
  - arbiter/mapping/test_market_map.py
---

<objective>
Let operators toggle `allow_auto_trade` on a MARKET_MAP entry from the dashboard without restarting the arbiter process. Mapping state reads are live so the AutoExecutor picks up the toggle on the next scanner emit.
</objective>

<tasks>

<task type="auto">
<name>Task 1: extend POST /api/market-mappings/{canonical_id}</name>
<files>arbiter/api.py, arbiter/mapping/market_map.py</files>
<action>
Extend the existing endpoint to accept an `action` field:
  - `action: "allow_auto_trade"` + `value: true|false` → updates MARKET_MAP[canonical_id].allow_auto_trade live.
  - `action: "confirm"` + review-status already exists — preserve.
  - `action: "reject"` — preserve.
  
Persist the change to the shared `market_map` store (not just in-memory Python dict) so a restart preserves operator toggles. Audit log entry: `{ts, actor_email, canonical_id, field, old, new}`.

Authentication: must be logged in as operator (existing session-token pattern in api.py).
</action>
</task>

<task type="auto">
<name>Task 2: unit tests for toggle + audit trail</name>
<files>arbiter/mapping/test_market_map.py</files>
<action>
- test_toggle_allow_auto_trade_true
- test_toggle_allow_auto_trade_false
- test_unauthenticated_rejects
- test_audit_entry_written_per_toggle
</action>
</task>

</tasks>

<success_criteria>
- [ ] Dashboard operator can flip allow_auto_trade on/off without restart
- [ ] Audit log captures who toggled what, when
- [ ] Unit tests cover the toggle + auth gate
</success_criteria>
