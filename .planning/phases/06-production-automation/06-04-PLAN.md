---
phase: 06-production-automation
plan: 04
type: execute
wave: 2
depends_on: []
autonomous: true
tags: [phase6, metrics, observability]
requirements: [OPS-06]
files_modified:
  - arbiter/api.py
  - arbiter/test_api_metrics.py
---

<objective>
Add /api/metrics endpoint (Prometheus text exposition format) + enrich /api/readiness to return go/no-go booleans matching the 15-item preflight checklist.
</objective>

<tasks>

<task type="auto">
<name>Task 1: /api/metrics (Prometheus text)</name>
<files>arbiter/api.py</files>
<action>
Add `handle_metrics(request)` returning `Content-Type: text/plain; version=0.0.4`:

```
# HELP arbiter_scanner_opportunities_total opportunities emitted by scanner
# TYPE arbiter_scanner_opportunities_total counter
arbiter_scanner_opportunities_total {count}
# HELP arbiter_executions_total trades executed
# TYPE arbiter_executions_total counter
arbiter_executions_total{status="filled"} {filled}
arbiter_executions_total{status="failed"} {failed}
# HELP arbiter_kill_switch_armed kill switch state
# TYPE arbiter_kill_switch_armed gauge
arbiter_kill_switch_armed {0|1}
# HELP arbiter_rate_limiter_tokens per-platform remaining tokens
# TYPE arbiter_rate_limiter_tokens gauge
arbiter_rate_limiter_tokens{platform="kalshi"} {n}
arbiter_rate_limiter_tokens{platform="polymarket"} {n}
# HELP arbiter_circuit_state circuit breaker state per collector
# TYPE arbiter_circuit_state gauge
arbiter_circuit_state{platform="kalshi"} {0=closed, 1=half_open, 2=open}
arbiter_circuit_state{platform="polymarket"} {0|1|2}
# HELP arbiter_orders_open currently open orders
# TYPE arbiter_orders_open gauge
arbiter_orders_open {count}
```

Wire up in api.py routes: `app.router.add_get("/api/metrics", self.handle_metrics)`.
</action>
</task>

<task type="auto">
<name>Task 2: /api/readiness enriched go/no-go</name>
<files>arbiter/api.py, arbiter/readiness.py</files>
<action>
Extend handle_readiness to return:
```json
{
  "ready": true|false,
  "checks": {
    "db_connected": true,
    "redis_connected": true,
    "kalshi_authenticated": true,
    "polymarket_authenticated": true,
    "telegram_configured": true,
    "mapping_has_allow_auto_trade": true,
    "kill_switch_disarmed": true,
    "no_circuit_open": true,
    "auto_execute_enabled": false  // info-only; not blocking
  },
  "blockers": []
}
```

`ready` is True only when all non-info checks pass.
</action>
</task>

<task type="auto">
<name>Task 3: unit tests</name>
<files>arbiter/test_api_metrics.py</files>
<action>
Test the /metrics formatter + /readiness aggregation with mocked services.
</action>
</task>

</tasks>

<success_criteria>
- [ ] `curl http://localhost:8080/api/metrics` returns valid Prometheus text
- [ ] `curl http://localhost:8080/api/readiness` returns structured go/no-go
- [ ] Unit tests cover the formatter + aggregation logic
</success_criteria>
